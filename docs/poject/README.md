# Документация проекта

Описание того, **как устроен проект сейчас**. Решения и причины, по которым он устроен именно так, лежат в [`docs/main`](../main): там ТЗ и дизайн-документы по этапам.

| Документ | О чём |
|---|---|
| [`api.md`](./api.md) | Справочник эндпоинтов: параметры, тела запросов и ответов, коды ошибок |
| [`structure.md`](./structure.md) | Структура каталогов, зависимости пакетов, схема БД, конфигурация |
| [`../main/tz.md`](../main/tz.md) | Исходное техническое задание |
| [`../main/design-stage1.md`](../main/design-stage1.md) | Дизайн Этапа 1 (MVP): решения, отступления от ТЗ, риски |
| [`../main/design-stage2.md`](../main/design-stage2.md) | Дизайн Этапа 2 (LLM-автозаполнение) |
| [`../../README.md`](../../README.md) | Запуск, настройка, разработка |

## Что это за приложение

Персональный трекер вакансий. Локальный однопользовательский инструмент: хранит вакансии, добавленные вручную по ссылке, статус прохождения по каждой и умеет проверять, не снята ли вакансия с публикации.

Работает на машине кандидата, без облачных зависимостей. В сеть выходит только по нажатию кнопки: проверить активность или заполнить карточку через языковую модель. Ничего не запускается по расписанию.

## Текущее состояние

### Этап 1 (MVP) — готов

- Добавление вакансии по ссылке, ручная правка карточки.
- Таблица с сортировкой по любому столбцу, неактивные скрыты по умолчанию.
- Статус кандидата: отклик, этап собеседования, контакт HR, зарплаты, сопроводительное письмо.
- Проверка активности по кнопке — одной вакансии или всех сразу.
- Ручное решение об активности сильнее результата проверки; расхождение показывается, но не исправляется само.

### Этап 2 (LLM-автозаполнение) — в работе

| Задача | Статус |
|---|---|
| 1. Дизайн-документ | готово |
| 2. Новые поля вакансии в backend: должность, вилка, формат работы | готово |
| 3. Те же поля в интерфейсе: форма, таблица, карточка | готово |
| 4. Конфигурация провайдеров, `GET /api/llm/providers` | готово |
| 5. Скачивание и очистка страницы вакансии | не начато |
| 6. Схема извлечения, валидация, таблица `llm_runs` | не начато |
| 7-8. Адаптеры Gemini, Claude, DeepSeek | не начато |
| 9-10. Журнал запусков и `POST /vacancies/{id}/extract` | не начато |
| 11-12. Интерфейс: выбор провайдера, предпросмотр, экран журнала | не начато |
| 13. Документация и финальная проверка | не начато |

Поиск вакансий по критериям через API источников отложен до Этапа 2.1: `api.hh.ru` больше не отдаёт вакансии анонимно, нужен токен приложения.

### Этап 3 — не начат

Нейроблок (прогноз зарплаты и соответствие рынку) и приём резюме собеседований из внешних систем. Таблицы `interview_summary` и `ai_block` уже созданы миграцией, но не используются; в карточке вакансии на их месте стоят заглушки.

## Объём кода и проверок

| Часть | Размер | Проверки |
|---|---|---|
| Backend (Go) | ~6 200 строк в 8 пакетах | 111 тестов: unit + интеграционные поверх in-memory SQLite |
| Frontend (React) | ~2 900 строк | 122 проверки в `npm run smoke` (рендер экранов в строку) |

Автоматических тестов на фронте нет по решению при проектировании: интерфейс проверяется руками, а смоук страхует от несобираемого состояния и битых импортов.

## Ключевые принципы, заложенные в код

**Ручное решение важнее автоматики.** Активность вакансии хранится в двух полях: результат проверки и решение пользователя. Отображаемое значение — `manual ?? auto ?? true`. Проверка никогда не перезатирает ручной выбор, только показывает расхождение.

**Ничего не происходит само.** Нет крона, нет фоновых задач. Каждый запрос к внешнему сайту и к языковой модели инициирует пользователь нажатием кнопки.

**Модель ничего не пишет напрямую.** Извлечённые поля возвращаются на предпросмотр, а запись идёт обычным `PATCH` после подтверждения. Путь записи в вакансию остаётся один.

**Нет авторизации — значит, нет и внешнего доступа.** Порт публикуется только на `127.0.0.1`. Это осознанное решение из ТЗ, а не упущение.
